[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"

[project]
name = "contextpulse-project"
version = "0.1.0"
description = "Auto-generated project context for AI agents"
requires-python = ">=3.12"
dependencies = [
    "contextpulse-core",
]

[tool.setuptools.packages.find]
where = ["src"]
